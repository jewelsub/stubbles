<ol class="tasks">
	<% tasks.each do |task| %>
		<%= render :partial => 'tasks/partials/view', :locals => {:task => task} %>
	<% end %>
</ol>
<br />
<%= link_to 'New Task', new_story_task_path(story), :class => 'button', 
	:remote => true, :'data-disable-with' => "Loading...",
	:style => 'margin: 10px 0px 10px 0px;' %>