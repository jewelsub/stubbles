<ul class="tasks">
	<% tasks.each do |task| %>
		<%= render :partial => 'tasks/partials/view', :locals => {:task => task} %>
	<% end %>
</ul>
<br />
<%= link_to 'New Task', new_story_task_path(story), :remote => true %>